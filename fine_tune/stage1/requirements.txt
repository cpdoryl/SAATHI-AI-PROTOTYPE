# SAATHI AI — Stage 1 LoRA Training Requirements
# Install: pip install -r requirements.txt
#
# Hardware: Requires NVIDIA GPU with >=24GB VRAM (A10G minimum, A100 recommended)
# CUDA: 11.8 or 12.1

# PyTorch (install separately first with CUDA version)
# pip install torch==2.1.0 --index-url https://download.pytorch.org/whl/cu118
# OR for CUDA 12.1:
# pip install torch==2.2.0 --index-url https://download.pytorch.org/whl/cu121

torch>=2.1.0
transformers>=4.40.0
datasets>=2.18.0
peft>=0.10.0
trl>=0.8.0
accelerate>=0.28.0
bitsandbytes>=0.43.0      # QLoRA 4-bit quantization

# Optional: flash-attention2 for faster training on A100
# pip install flash-attn --no-build-isolation

# Monitoring
wandb>=0.16.0             # Training tracking (optional)

# Evaluation
sacrebleu>=2.3.1          # BLEU score (optional, we have fallback)
rouge-score>=0.1.2        # ROUGE (optional, we have fallback)

# Utilities
scipy>=1.11.0
numpy>=1.24.0
loguru>=0.7.0
tqdm>=4.66.0
