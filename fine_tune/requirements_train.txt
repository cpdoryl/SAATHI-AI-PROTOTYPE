"""
SAATHI AI — Fine-tuning Dependencies
Install with: pip install -r fine_tune/requirements_train.txt
"""

# Core ML
torch>=2.3.1
torchvision
torchaudio

# HuggingFace stack
transformers>=4.44.0
peft>=0.12.0
trl>=0.9.4
datasets>=2.21.0
accelerate>=0.33.0
tokenizers>=0.19.0

# Quantization (QLoRA) — Windows CUDA compatible
bitsandbytes>=0.43.0

# Utilities
sentencepiece
safetensors
huggingface-hub>=0.24.0

# Optional: faster training (Linux preferred but works on Windows CUDA)
# xformers  -- only uncomment if on Linux/WSL2
